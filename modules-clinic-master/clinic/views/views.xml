<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="clinic.list">
      <field name="name">clinic list</field>
      <field name="model">clinic.clinic</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="clinic.action_window">
      <field name="name">clinic window</field>
      <field name="res_model">clinic.clinic</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="clinic.action_server">
      <field name="name">clinic server</field>
      <field name="model_id" ref="model_clinic_clinic"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="clinic" id="clinic.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="clinic.menu_1" parent="clinic.menu_root"/>
    <menuitem name="Menu 2" id="clinic.menu_2" parent="clinic.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="clinic.menu_1_list" parent="clinic.menu_1"
              action="clinic.action_window"/>
    <menuitem name="Server to list" id="clinic" parent="clinic.menu_2"
              action="clinic.action_server"/>
    -->
  </data>
</odoo>